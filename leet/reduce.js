var reduce = function(nums, fn, init) {
   return nums.reduce(fn)
};


// composedFunction

const add =(a,b)=>a+b
const mul=(a,b)=>a*b
const sub =(a,b)=>a-b

function composedFunction(add,mul,sum){

}
